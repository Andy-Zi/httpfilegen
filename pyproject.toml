[project]
name = "httpfilegen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openapi-spec-validator>=0.7.2",
    "prance>=25.4.8.0",
]
